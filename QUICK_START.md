# 快速启动指南（移动端专用）

**📱 本项目为移动端（手机/平板）专用，不兼容PC端。**

## 环境要求

- **Cocos Creator 3.8.0+** (推荐最新版本)
- **Node.js 14+**
- **Android SDK** 或 **Xcode**（用于构建移动端应用）

## 导入步骤

### 1. 在 Cocos Creator 中打开项目

1. 打开 **Cocos Dashboard**
2. 点击 **添加** 或 **Import**
3. 选择此项目文件夹
4. 等待 Cocos Creator 自动生成 `.meta` 文件

### 2. 创建游戏场景

1. 在 **Assets** 面板右键 → **Create** → **Scene**
2. 命名为 `GameScene`
3. 双击打开场景

### 3. 设置场景 (自动方式)

在场景中添加一个空节点并挂载 `SceneSetup` 组件，它会自动创建所有游戏元素：

1. 在 **Hierarchy** 面板右键 → **Create Empty Node**
2. 命名为 `GameSetup`
3. 在 **Inspector** 面板点击 **Add Component**
4. 搜索并添加 `SceneSetup`
5. 勾选 `createMobileUI` 启用移动端UI
6. 运行游戏！

### 4. 设置场景 (手动方式)

如果需要更多控制，可以手动设置：

#### 4.1 创建 Canvas
```
Hierarchy → 右键 → Create → UI → Canvas
```

#### 4.2 添加游戏管理器
```
Canvas 下创建空节点 "GameManager"
添加组件: GameManager, LevelManager
```

#### 4.3 添加输入管理器（移动端触摸控制）
```
Canvas 下创建空节点 "InputManager"
添加组件: InputManager
配置属性:
  - touchDivider: 0.5 (屏幕中间分界)
  - flipperZoneHeight: 0.4 (底部40%为触摸区域)
  - skillButtonRadius: 80 (技能按钮半径)
  - skillButtonOffsetY: 150 (距底部距离)
  - vibrationEnabled: true (震动反馈)
```

#### 4.4 添加移动端UI
```
Canvas 下创建空节点 "MobileUI"
添加组件: MobileUI
配置属性:
  - showTouchZones: true (显示触摸区域指示)
  - skillButtonSize: 120 (技能按钮大小)
```

#### 4.5 添加伤害系统
```
Canvas 下创建空节点 "DamageSystem"
添加组件: DamageSystem
```

#### 4.6 添加队伍战斗
```
Canvas 下创建空节点 "TeamBattle"
添加组件: TeamBattle
```

#### 4.7 创建角色和挡板
SceneSetup 组件会自动创建这些，或参考 SceneSetup.ts 手动创建。

## 物理设置

确保项目物理设置正确：

1. 打开 **Project Settings** → **Physics 2D**
2. 设置 **Gravity Y** = `-500`
3. 启用 **Enable Physics**

## 移动端构建设置

### Android 构建
1. **Project** → **Build** → 选择 **Android**
2. 设置 **包名**: `com.yourcompany.worldflipper`
3. 设置 **屏幕方向**: **Portrait**（竖屏）
4. 启用 **Multi-touch**（多点触控）
5. 点击 **Build** 然后 **Make**

### iOS 构建
1. **Project** → **Build** → 选择 **iOS**
2. 设置 **Bundle Identifier**
3. 设置 **屏幕方向**: **Portrait**（竖屏）
4. 在 Xcode 中打开生成的项目进行签名和发布

## 场景层级结构示例

```
Canvas
├── GameManager          [GameManager, LevelManager]
├── InputManager         [InputManager] (移动端触摸控制)
├── MobileUI             [MobileUI] (移动端虚拟按钮)
├── DamageSystem         [DamageSystem]
├── TeamBattle           [TeamBattle]
├── Walls
│   ├── LeftWall         [Wall]
│   ├── RightWall        [Wall]
│   └── TopWall          [Wall]
├── Flippers
│   ├── LeftFlipper      [Flipper]
│   └── RightFlipper     [Flipper]
├── DeadZone             [DeadZone]
├── Enemies              [容器节点]
└── UI
    └── GameUI           [GameUI]
```

## UI 面板使用

各 UI 面板可以单独创建和测试：

```typescript
// 主界面
const mainMenu = new Node('MainMenu');
mainMenu.addComponent(MainMenuUI);

// 角色面板
const charPanel = new Node('CharacterPanel');
charPanel.addComponent(CharacterPanel);

// 背包面板
const invPanel = new Node('InventoryPanel');
invPanel.addComponent(InventoryPanel);

// 关卡选择
const levelSelect = new Node('LevelSelect');
levelSelect.addComponent(LevelSelectPanel);

// 领主战
const bossRaid = new Node('BossRaid');
bossRaid.addComponent(BossRaidPanel);
```

## 测试运行

### 在编辑器中测试（模拟器）
1. 点击编辑器顶部的 **▶ 播放** 按钮
2. 在 **Preview** 窗口选择移动设备模拟
3. 使用鼠标模拟触摸：
   - 点击屏幕左半边控制左挡板
   - 点击屏幕右半边控制右挡板
   - 点击底部中央技能按钮释放技能

### 在真机上测试
1. 构建 Android APK 或 iOS 应用
2. 安装到手机上运行
3. 触摸屏幕左右两侧控制挡板
4. 点击技能按钮释放技能

## 常见问题

### Q: 打开项目报错？
A: 确保使用 Cocos Creator 3.8.0 或更高版本。

### Q: 物理不工作？
A: 检查 Project Settings → Physics 2D 设置。

### Q: 看不到角色？
A: 确保场景有 Camera 组件，且角色在相机视野内。

### Q: 脚本找不到？
A: 等待 Cocos Creator 完成资源导入（查看底部进度条）。

### Q: 触摸不响应？
A: 确保 InputManager 组件已添加到场景中，并检查触摸区域设置。

### Q: 震动不工作？
A: 震动需要真机测试，且需要设备支持 Vibration API。在 InputManager 中可关闭震动反馈。

### Q: 多点触控不工作？
A: 确保在构建设置中启用了 Multi-touch，并且在 InputManager 中 `multiTouchEnabled` 为 true。

## 移动端优化建议

1. **屏幕适配**: 使用 Canvas 的 Fit Height 或 Fit Width 模式
2. **触摸响应**: 适当调整 `flipperZoneHeight` 确保触摸区域足够大
3. **性能优化**: 控制同屏敌人数量，使用对象池
4. **电量优化**: 控制帧率在 60fps 以下
5. **震动反馈**: 根据用户偏好提供关闭选项

## 下一步

- 替换程序化绘制为真实美术资源
- 添加音效和背景音乐
- 完善游戏流程
- 添加新手引导
- 优化移动端性能
